import 'package:flutter/material.dart';
import 'package:flutter_template/res.dart';

class ResTextStyles {
  TextStyle headline1({ThemeMode? themeMode}) => TextStyle(
        fontFamily: 'flutter_template',
        fontSize: 35,
        fontWeight: FontWeight.w400,
        color: Res.colors.text(themeMode: themeMode),
      );

  TextStyle headline2({ThemeMode? themeMode}) => TextStyle(
        fontFamily: 'flutter_template',
        fontSize: 32,
        fontWeight: FontWeight.w400,
        color: Res.colors.text(themeMode: themeMode),
      );

  TextStyle headline3({ThemeMode? themeMode}) => TextStyle(
        fontFamily: 'flutter_template',
        fontSize: 29,
        fontWeight: FontWeight.w400,
        color: Res.colors.text(themeMode: themeMode),
      );

  TextStyle headline4({ThemeMode? themeMode}) => TextStyle(
        fontFamily: 'flutter_template',
        fontSize: 26,
        fontWeight: FontWeight.w400,
        color: Res.colors.text(themeMode: themeMode),
      );

  TextStyle headline5({ThemeMode? themeMode}) => TextStyle(
        fontFamily: 'flutter_template',
        fontSize: 23,
        fontWeight: FontWeight.w400,
        color: Res.colors.text(themeMode: themeMode),
      );

  TextStyle headline6({ThemeMode? themeMode}) => TextStyle(
        fontFamily: 'flutter_template',
        fontSize: 20,
        fontWeight: FontWeight.w400,
        color: Res.colors.text(themeMode: themeMode),
      );

  TextStyle subtitle1({ThemeMode? themeMode}) => TextStyle(
        fontFamily: 'flutter_template',
        fontSize: 18,
        fontWeight: FontWeight.w400,
        color: Res.colors.text(themeMode: themeMode),
      );

  TextStyle subtitle2({ThemeMode? themeMode}) => TextStyle(
        fontFamily: 'flutter_template',
        fontSize: 16,
        fontWeight: FontWeight.w400,
        color: Res.colors.text(themeMode: themeMode),
      );

  TextStyle body1({ThemeMode? themeMode}) => TextStyle(
        fontFamily: 'flutter_template',
        fontSize: 15,
        fontWeight: FontWeight.w400,
        color: Res.colors.text(themeMode: themeMode),
      );

  TextStyle body2({ThemeMode? themeMode}) => TextStyle(
        fontFamily: 'flutter_template',
        fontSize: 13,
        fontWeight: FontWeight.w400,
        color: Res.colors.text(themeMode: themeMode),
      );

  TextStyle caption({ThemeMode? themeMode}) => TextStyle(
        fontFamily: 'flutter_template',
        fontSize: 11,
        fontWeight: FontWeight.w400,
        color: Res.colors.text(themeMode: themeMode),
      );

  TextStyle button({ThemeMode? themeMode}) => TextStyle(
        fontFamily: 'flutter_template',
        fontSize: 16,
        fontWeight: FontWeight.w400,
        color: Res.colors.text(themeMode: themeMode),
      );
}
